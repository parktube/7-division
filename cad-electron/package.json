{
  "name": "ai-native-cad",
  "version": "0.1.0",
  "private": true,
  "description": "AI-Native CAD - Electron app for real-time scene.json rendering",
  "main": "./out/main/index.js",
  "scripts": {
    "dev": "node scripts/run-electron-vite.cjs dev",
    "build": "node scripts/run-electron-vite.cjs build",
    "preview": "node scripts/run-electron-vite.cjs preview",
    "prepare:cad-tools": "npm --prefix ../cad-tools run build",
    "package": "npm run prepare:cad-tools && npm run build && electron-builder",
    "package:mac": "npm run prepare:cad-tools && npm run build && electron-builder --mac",
    "package:win": "npm run prepare:cad-tools && npm run build && electron-builder --win",
    "package:linux": "npm run prepare:cad-tools && npm run build && electron-builder --linux"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.10.0",
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0",
    "electron-vite": "^2.3.0",
    "rollup-plugin-copy": "^3.5.0",
    "typescript": "^5.3.0",
    "vite": "^5.4.0"
  }
}
